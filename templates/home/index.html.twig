{% extends 'base.html.twig' %}
{% block title %}EatYV
{% endblock %}
{% block body %}
	{% if app.user %}
	{% include('nav.html.twig') %}
	<div class="container">
		<div class="row">
		</div>
	</div>
		{% else %}

			<div class="background-image">
				<div class="small-6 small-centered columns">
					<a href="{{ path('app_register') }}" class="btn btn-orange">Get Started</a>
					<a href="{{ path('app_login') }}" class="btn btn-orange">Sign In</a>

				</div>
			</div>
	{% endif %}

{% endblock %}
