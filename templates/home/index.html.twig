{% extends 'base.html.twig' %}

{% block title %}EatYV
{% endblock %}
{% block body %}
	{% if app.user %}
    <a href="{{ path('app_logout') }}" class="btn btn-orange">Logout</a>
	
		{% else %}

			<div class="background-image">
				<div class="small-6 small-centered columns">
					<a href="{{ path('app_register') }}" class="btn btn-orange">Get Started</a>
					<a href="{{ path('app_login') }}" class="btn btn-orange">Sign In</a>

				</div>
			</div>
	{% endif %}

{% endblock %}
